include "cluster"
include "discovery"

akka {
  loglevel = DEBUG
}

akka.http.server.preview.enable-http2 = on

shopping-cart {

  create-tables = false

  grpc {
    interface = "127.0.0.1"
    port = 8101
  }
  askTimeout = 5 s
}
